import {BeatLoader} from 'react-spinners'

const Loader = () => {
    return (
        <div className="spinner-wrapper">
            <BeatLoader size={20} />
        </div>
    );
};

export default Loader;